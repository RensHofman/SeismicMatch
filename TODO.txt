data_handler:    construct data path if not all fields are provided    create_templates:    match with existing would be an interesting option    match_templates:    config:    perpare_data_list:    example_data:template_matching:    create_template_families:    test on larger dataset    create_catalog:    for second iteration    common:setup:    depencencies    channels    tests:    testdata?general:    logging from multiprocessing    Licence    allow multiple channel cc    check if common functions are common    get rid of global?    GitHub